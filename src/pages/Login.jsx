import { FormLoginComponent } from '../components/FormLogin';

const FormLogin = () => {
    return (
        <div>
            <FormLoginComponent />
        </div>
    );
};

export default FormLogin;
